<?xml version="1.0" encoding="utf-8"?>
<!--
  No external package sources. Every project here references only the shared
  framework, which keeps restore offline and the supply chain empty. Add a
  source deliberately when a package is genuinely needed.
-->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
